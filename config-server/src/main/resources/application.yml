server:
  port: 8880

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LumyaZ/config-server-java-exam-final.git
          default-label: master